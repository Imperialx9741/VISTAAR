# TEMPLATE — not a real Secret (ADR-0053). Copy to grafana-secret.yaml,
# fill in a real admin password, and apply directly — do NOT commit the
# filled-in file, same "prefer kubectl create secret / a real secret
# manager over checking in filled-in YAML" guidance backend-secret
# .example.yaml already gives.
apiVersion: v1
kind: Secret
metadata:
  name: vistaar-grafana-secret
  namespace: vistaar
type: Opaque
stringData:
  admin-user: "admin"
  admin-password: "REPLACE_ME"
