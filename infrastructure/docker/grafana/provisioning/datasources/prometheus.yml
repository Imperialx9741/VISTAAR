# Auto-provisions the Prometheus data source on Grafana startup — no
# manual "Add data source" click-through needed for local dev (ADR-0053).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
